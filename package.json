{
  "version": 2,
 
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app.py"
    }
  ],
  "env": {
    "FLASK_ENV": "production",
    "GUNICORN_CMD_ARGS": "--workers 1 --bind 0.0.0.0:5000"
  }
}
